    #include <bits/stdc++.h>

    using namespace std;

    int dp[1002][10000];

    int main() {
        ios_base::sync_with_stdio(0);
        cin.tie(0);
        
        int n;
        cin >> n;
        if(n%2 == 1) cout << "CY";
        else cout << "SK";
        
    }